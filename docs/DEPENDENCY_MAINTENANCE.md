# Dependency maintenance

The npm lockfile was refreshed after Sprint 3.2 using Node.js 24 and `npm install --package-lock-only --ignore-scripts` so it matches the current `package.json`, including the document-ingestion dependencies.
