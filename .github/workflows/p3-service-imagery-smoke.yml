name: Archived P3 service imagery production smoke

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'P3 service imagery production smoke passed on 11 Aug 2026; workflow archived intentionally.'
