name: Archived approved service image importer

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Service image import completed 11 Aug 2026; workflow archived intentionally.'
