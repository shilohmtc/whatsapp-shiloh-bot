const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');
const root=path.resolve(__dirname,'..');const interactive=fs.readFileSync(path.join(root,'src/services/adminInteractiveMenu.js'),'utf8');const buttons=fs.readFileSync(path.join(root,'src/services/adminEarningsButtons.js'),'utf8');
test('generic Earnings opens a role-authorized scope and period picker',()=>{assert.match(interactive,/function earningsInteractive\(admin\)/);assert.match(interactive,/function earningsPeriodInteractive\(subject\)/);assert.match(interactive,/admin_earnings_subject_/);});
test('new generic flow exposes all four periods and old named IDs only redirect',()=>{for(const period of ['today','week','last_week','month'])assert.match(interactive,new RegExp(`\\['${period}'|${period}`));for(const kind of ['abigail','christel'])assert.match(buttons,new RegExp(`admin_${kind}_earnings_last_week: 'admin_retired_named_earnings'`));});
