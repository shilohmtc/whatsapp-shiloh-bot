const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');
const root=path.resolve(__dirname,'..');const interactive=fs.readFileSync(path.join(root,'src/services/adminInteractiveMenu.js'),'utf8');const buttons=fs.readFileSync(path.join(root,'src/services/adminEarningsButtons.js'),'utf8');
test('reports earnings actions open period picker instead of defaulting to today',()=>{assert.match(interactive,/abigailEarningsButtons, christelEarningsButtons/);assert.match(interactive,/action\.key === 'abigail_earnings'.*abigailEarningsButtons\(\)/s);assert.match(interactive,/action\.key === 'christel_earnings'.*christelEarningsButtons\(\)/s);});
test('earnings period picker exposes today week last week and month for both practitioners',()=>{for(const kind of ['abigail','christel']){for(const period of ['today','week','last_week','month'])assert.match(buttons,new RegExp(`admin_${kind}_earnings_${period}`));}});
