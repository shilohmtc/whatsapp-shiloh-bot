const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');
const source=p=>fs.readFileSync(path.join(__dirname,'..',p),'utf8');
test('legacy admin template-test endpoint is explicitly retired without sending',()=>{const controller=source('src/controllers/adminController.js');const handler=controller.match(/exports\.sendTemplateTest[\s\S]*?\n\}\);/);assert.ok(handler);assert.match(handler[0],/status\(410\)/);assert.match(handler[0],/read-only Meta template inventory/);assert.doesNotMatch(handler[0],/sendWhatsAppTemplate/);});
test('controlled lifecycle message endpoint is explicitly retired without creating evidence',()=>{const controller=source('src/controllers/appointmentLifecycleController.js');const handler=controller.match(/exports\.runControlledLifecycleTest[\s\S]*?\n\}\);/);assert.ok(handler);assert.match(handler[0],/status\(410\)/);assert.match(handler[0],/Genuine lifecycle evidence/);assert.doesNotMatch(handler[0],/createAppointment|sendWhatsAppTemplate|createPendingExperience/);});
