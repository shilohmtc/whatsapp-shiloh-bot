const test=require('node:test');const assert=require('node:assert/strict');const fs=require('fs');
test('service content migrations are presentation-only',()=>{for(const f of ['migrations/038_service_customer_content.sql','migrations/039_service_customer_descriptions.sql']){const s=fs.readFileSync(f,'utf8');assert.doesNotMatch(s,/DELETE\s+FROM\s+(appointments|clients)/i);assert.doesNotMatch(s,/UPDATE\s+(appointments|clients)/i);}const seed=fs.readFileSync('migrations/039_service_customer_descriptions.sql','utf8');assert.match(seed,/Customer description seed incomplete/);});
test('catalogue is active CRM only and WhatsApp-linked',()=>{const s=fs.readFileSync('src/services/serviceCatalogue.js','utf8'),r=fs.readFileSync('src/routes/services.js','utf8'),a=fs.readFileSync('app.js','utf8');assert.match(s,/s\.status='active'/);assert.match(r,/Book this treatment via WhatsApp/);assert.match(a,/serviceRoutes/);});
test('guarded catalogue migration refuses unrelated pending migrations',()=>{const s=fs.readFileSync('src/services/serviceCatalogueMigration.js','utf8');assert.match(s,/unexpected migrations are pending/);assert.match(s,/038_service_customer_content\.sql/);assert.match(s,/039_service_customer_descriptions\.sql/);});
