const { pool } = require('../db/pool');
const { normalizePhone } = require('./clientIdentityOnboarding');
const { processAdminHolidayHoursMessage, getHolidayReminder } = require('./adminHolidayHours');
const { processAdminFreelancerAvailabilityMessage } = require('./adminFreelancerAvailability');
const { processAdminMobileBookingFlowMessage } = require('./adminMobileBookingFlow');
const { processAdminStaffScheduleFlowMessage } = require('./adminStaffScheduleFlow');
const { processAdminOwnScheduleMessage } = require('./adminOwnSchedule');
const { processAdminAppointmentCancellationMessage } = require('./adminAppointmentCancellation');
const { processAdminStaffServicesMessage } = require('./adminStaffServices');
const { processAdminServicePricingMessage } = require('./adminServicePricing');
const { processAdminBookingUpdateMessage } = require('./adminBookingUpdate');
const { processAdminReportsMessage } = require('./adminReports');
const { processAdminChristelEarningsMessage } = require('./adminChristelEarnings');
const { abigailEarningsButtons, christelEarningsButtons } = require('./adminEarningsButtons');
const { findClients, formatClientLookupReply } = require('./adminClientLookup');
const { filterClientsForAdminScope } = require('./staffAdminScope');
const moreSessions = new Map();
function has(admin,p){return admin?.permissions?.[p]===true;}function isGreeting(text=''){return /^(hi|hello|hey|howzit|hiya|good morning|good afternoon|good evening)[!. ]*$/i.test(String(text).trim());}function senderKey(sender){return normalizePhone(sender);}function isBusinessWide(admin){return ['owner','business_admin'].includes(admin?.business_role)||admin?.calendar_scope==='all_business';}function isTenant(admin){return admin?.business_role==='tenant_practitioner';}function isEmployeePractitioner(admin){return admin?.business_role==='employee_practitioner';}function isChristelAdmin(admin){return isBusinessWide(admin)&&/^christel\b/i.test(String(admin?.display_name||'').trim());}
async function getAdmin(sender){const r=await pool.query(`SELECT id,staff_id,display_name,role,permissions,service_scope,business_role,calendar_scope FROM staff_admin_accounts WHERE normalized_whatsapp=$1 AND active=TRUE`,[senderKey(sender)]);return r.rows[0]||null;}async function audit(id,action,metadata={}){await pool.query(`INSERT INTO crm_audit_events (actor_admin_id,action,entity_type,entity_id,metadata) VALUES ($1,$2,'admin_assistant',NULL,$3::jsonb)`,[id,action,JSON.stringify(metadata)]);}async function scopedClientLookup(admin,q){const found=await findClients(q);const clients=await filterClientsForAdminScope(admin,found.clients);await audit(admin.id,'admin.client_lookup',{queryType:found.queryType,resultCount:clients.length,resultClientIds:clients.map(c=>c.id),scoped:!isBusinessWide(admin)});return formatClientLookupReply(q,clients);}
function getMenuOptions(admin){const options=[];if(has(admin,'appointment:view')){options.push({key:'today',label:isBusinessWide(admin)?"Today's clients":"My clients today",section:'Appointments'});options.push({key:'tomorrow',label:isBusinessWide(admin)?"Tomorrow's clients":"My clients tomorrow",section:'Appointments'});if(has(admin,'appointment:create'))options.push({key:'availability',label:'Find an available time',section:'Appointments'});options.push({key:'today_report',label:isBusinessWide(admin)?"Today's report":"My report today",section:'Reports'});if(isChristelAdmin(admin))options.push({key:'christel_earnings',label:'💰 Christel earnings',section:'Reports'});if(isBusinessWide(admin))options.push({key:'abigail_earnings',label:'💰 Abigail earnings',section:'Reports'});}if(has(admin,'appointment:create')&&has(admin,'appointment:view'))options.push({key:'booking',label:'Make a booking',section:'Appointments'});if(has(admin,'booking:update')&&has(admin,'appointment:view'))options.push({key:'manage_booking',label:'Manage a booking',section:'Appointments'});if(has(admin,'client:lookup'))options.push({key:'client',label:isBusinessWide(admin)?'Find a client':'Find my client',section:'Clients'});if(has(admin,'walkin:create'))options.push({key:'walkin',label:'Add a walk-in',section:'Clients'});if(has(admin,'staff:services:view'))options.push({key:'staff_services',label:isBusinessWide(admin)?'Staff services':'My services',section:'Services'});if(has(admin,'service:pricing'))options.push({key:'pricing',label:isTenant(admin)?'My services & pricing':'Services & pricing',section:'Services'});if(has(admin,'schedule:manage'))options.push({key:'schedule',label:isTenant(admin)?'My schedule':'Schedule management',section:'Schedule'});options.push({key:'help',label:'Help',section:'More'});return options.map((o,i)=>({...o,number:i+1}));}
function menu(admin){const roleLine=isTenant(admin)?'Tenant business access — your clients, services and diary only.':isEmployeePractitioner(admin)?'Practitioner access — your diary and assigned client work only.':'';const options=getMenuOptions(admin);const lines=['*Shiloh Admin 🌿*',`Welcome back, ${admin.display_name} 👋`];if(roleLine)lines.push(roleLine);lines.push('','What would you like to do?');let section=null;for(const o of options){if(o.section!==section){section=o.section;lines.push('',`*${section}*`);}lines.push(`${o.number}️⃣ ${o.label}`);}lines.push('','Reply with the number or option name.');return lines.join('\n');}
function optionFor(admin,v){const options=getMenuOptions(admin);if(/^\d+$/.test(v))return options.find(o=>o.number===Number(v))||null;return options.find(o=>o.label.toLowerCase()===v)||null;}function clientGuide(admin){return [isBusinessWide(admin)?'*Find a client*':'*Find my client*','',isBusinessWide(admin)?'Send the client name or mobile number after “Find client”.':'Search is limited to clients in your authorized practitioner/service scope.','','Example:','Find client Chenique'].join('\n');}function scheduleMenu(admin){if(isTenant(admin))return ['*My schedule*','','1️⃣ My regular hours','2️⃣ My leave / special availability','','0️⃣ Back','','Reply with a number or option name.'].join('\n');return ['*Schedule management*','','1️⃣ Staff hours','2️⃣ Leave / special availability','3️⃣ Freelancer availability','4️⃣ Holiday hours','','0️⃣ Back','','Reply with a number or option name.'].join('\n');}function returnedToMore(reply=''){return /\*(?:More — )?Schedule management\*/i.test(String(reply));}
async function processAdminMobileMenuMessage(sender,text){const k=senderKey(sender);const admin=await getAdmin(sender);if(!admin)return {handled:false};const raw=String(text||'').trim();const v=raw.toLowerCase().replace(/\s+/g,' ');if(isTenant(admin)){const ownSchedule=await processAdminOwnScheduleMessage(sender,text);if(ownSchedule.handled){moreSessions.delete(k);return ownSchedule;}}const christelEarnings=await processAdminChristelEarningsMessage(sender,text);if(christelEarnings.handled){moreSessions.delete(k);return christelEarnings;}const report=await processAdminReportsMessage(sender,text);if(report.handled){moreSessions.delete(k);return report;}const clientCommand=raw.match(/^(?:find|lookup|search(?: for)?)\s+client\s+(.+)$/i)||raw.match(/^client\s+(?:find|lookup|search)\s+(.+)$/i);if(clientCommand&&has(admin,'client:lookup'))return{handled:true,admin,reply:await scopedClientLookup(admin,clientCommand[1].trim())};const bookingUpdateFlow=await processAdminBookingUpdateMessage(sender,text);if(bookingUpdateFlow.handled){moreSessions.delete(k);return bookingUpdateFlow;}const pricingFlow=await processAdminServicePricingMessage(sender,text);if(pricingFlow.handled){moreSessions.delete(k);return pricingFlow;}const cancellationFlow=await processAdminAppointmentCancellationMessage(sender,text);if(cancellationFlow.handled){moreSessions.delete(k);return cancellationFlow;}const bookingFlow=await processAdminMobileBookingFlowMessage(sender,text);if(bookingFlow.handled){moreSessions.delete(k);return bookingFlow;}const more=moreSessions.get(k);if(more?.step==='menu'){if(v==='0'||v==='back'){moreSessions.delete(k);return {handled:true,admin,reply:menu(admin)};}if(v==='1'||v==='staff hours'||v==='my regular hours'){moreSessions.delete(k);return isTenant(admin)?processAdminOwnScheduleMessage(sender,'My regular hours'):processAdminStaffScheduleFlowMessage(sender,'Staff hours');}if(v==='2'||v==='leave / special availability'||v==='leave'||v==='special availability'||v==='my leave / special availability'){moreSessions.delete(k);return isTenant(admin)?processAdminOwnScheduleMessage(sender,'My leave'):processAdminStaffScheduleFlowMessage(sender,'Leave / special availability');}if(!isTenant(admin)&&v==='3'){moreSessions.delete(k);return processAdminFreelancerAvailabilityMessage(sender,'Freelancer availability');}if(!isTenant(admin)&&v==='4'){moreSessions.delete(k);return processAdminHolidayHoursMessage(sender,'Holiday hours');}return {handled:true,admin,reply:isTenant(admin)?'Choose 1, 2, or 0.':'Choose 1, 2, 3, 4, or 0.'};}if(!isTenant(admin)){const staffFlow=await processAdminStaffScheduleFlowMessage(sender,text);if(staffFlow.handled){if(staffFlow.returnToMore||returnedToMore(staffFlow.reply)){moreSessions.set(k,{step:'menu'});return {handled:true,admin:staffFlow.admin,reply:scheduleMenu(admin)};}return staffFlow;}}const holiday=await processAdminHolidayHoursMessage(sender,text);if(holiday.handled){if(isTenant(admin))return {handled:true,admin,reply:'Holiday hours are managed by the Shiloh business owner/admin. You can manage your own working hours and exceptions.'};if(returnedToMore(holiday.reply))moreSessions.set(k,{step:'menu'});return holiday;}const freelancer=await processAdminFreelancerAvailabilityMessage(sender,text);if(freelancer.handled){if(isTenant(admin))return {handled:true,admin,reply:'Freelancer availability is managed by the Shiloh business owner/admin.'};if(returnedToMore(freelancer.reply))moreSessions.set(k,{step:'menu'});return freelancer;}if(['menu','admin menu','home'].includes(v)||isGreeting(raw)){moreSessions.delete(k);await audit(admin.id,'admin.mobile_menu_viewed',{entry:isGreeting(raw)?'greeting':'menu',businessRole:admin.business_role});const reminder=isBusinessWide(admin)&&has(admin,'schedule:manage')?await getHolidayReminder():null;return {handled:true,admin,reply:reminder?`${menu(admin)}\n\n${reminder}`:menu(admin)};}const selected=optionFor(admin,v);if(selected){await audit(admin.id,'admin.mobile_menu_selected',{option:selected.key,businessRole:admin.business_role});if(selected.key==='today'||selected.key==='tomorrow')return {handled:false};if(selected.key==='today_report')return processAdminReportsMessage(sender,isBusinessWide(admin)?"Today's report":'My report today');if(selected.key==='christel_earnings')return {handled:true,admin,interactive:christelEarningsButtons()};if(selected.key==='abigail_earnings')return {handled:true,admin,interactive:abigailEarningsButtons()};if(selected.key==='availability')return processAdminMobileBookingFlowMessage(sender,'Find an available time');if(selected.key==='booking')return processAdminMobileBookingFlowMessage(sender,'Make a booking');if(selected.key==='manage_booking')return processAdminBookingUpdateMessage(sender,'Manage booking');if(selected.key==='client')return {handled:true,admin,reply:clientGuide(admin)};if(selected.key==='walkin')return {handled:false};if(selected.key==='staff_services')return processAdminStaffServicesMessage(sender,'Staff services');if(selected.key==='pricing')return processAdminServicePricingMessage(sender,'Manage services & pricing');if(selected.key==='schedule'){moreSessions.set(k,{step:'menu'});return {handled:true,admin,reply:scheduleMenu(admin)};}if(selected.key==='help')return {handled:false};}if(v==='today'||v==="today's clients"||v==='todays clients'||v==='my clients today'||v==='tomorrow'||v==="tomorrow's clients"||v==='tomorrows clients'||v==='my clients tomorrow')return {handled:false};if(v==='find an available time'||v==='find availability')return processAdminMobileBookingFlowMessage(sender,'Find an available time');if(['make a booking','new booking','book client','book a client','book appointment'].includes(v))return processAdminMobileBookingFlowMessage(sender,'Make a booking');if(['manage a booking','manage booking','update booking','edit booking'].includes(v))return processAdminBookingUpdateMessage(sender,'Manage booking');if(v==='find a client'||v==='find my client'){if(!has(admin,'client:lookup'))return {handled:false};return {handled:true,admin,reply:clientGuide(admin)};}if(v==='add a walk-in'||v==='add walk-in')return {handled:false};if(['staff services','my services','services by staff','services per staff'].includes(v))return processAdminStaffServicesMessage(sender,'Staff services');if(['services & pricing','my services & pricing','manage services & pricing','service pricing','pricing'].includes(v))return processAdminServicePricingMessage(sender,'Manage services & pricing');if(['schedule management','my schedule','staff schedule','schedule'].includes(v)){if(!has(admin,'schedule:manage'))return {handled:false};moreSessions.set(k,{step:'menu'});return {handled:true,admin,reply:scheduleMenu(admin)};}if(v==='staff hours'||v==='regular staff hours'||v==='my regular hours')return isTenant(admin)?processAdminOwnScheduleMessage(sender,'My regular hours'):processAdminStaffScheduleFlowMessage(sender,'Staff hours');if(['leave','leave / special availability','my leave / special availability','leave and special availability','special availability'].includes(v))return isTenant(admin)?processAdminOwnScheduleMessage(sender,'My leave'):processAdminStaffScheduleFlowMessage(sender,'Leave / special availability');if(isTenant(admin)&&/^(working hours|set working hours|add schedule exception|remove schedule exception)\b/i.test(raw))return{handled:true,admin,reply:'For privacy and safety, your tenant account can only change your own schedule. Send *My schedule* for the scoped commands.'};if(['freelancer availability','freelancers','freelance schedule'].includes(v)){if(!isBusinessWide(admin))return {handled:true,admin,reply:'Freelancer availability is managed by the Shiloh business owner/admin.'};return processAdminFreelancerAvailabilityMessage(sender,'Freelancer availability');}if(v==='holiday hours'||v==='public holiday hours'){if(!isBusinessWide(admin))return {handled:true,admin,reply:'Holiday hours are managed by the Shiloh business owner/admin.'};return processAdminHolidayHoursMessage(sender,'Holiday hours');}if(v==='help')return {handled:false};return {handled:false};}
module.exports={processAdminMobileMenuMessage,menu,getMenuOptions};
